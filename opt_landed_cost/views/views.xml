<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="opt_landed_cost.list">
      <field name="name">opt_landed_cost list</field>
      <field name="model">opt_landed_cost.opt_landed_cost</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="opt_landed_cost.action_window">
      <field name="name">opt_landed_cost window</field>
      <field name="res_model">opt_landed_cost.opt_landed_cost</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="opt_landed_cost.action_server">
      <field name="name">opt_landed_cost server</field>
      <field name="model_id" ref="model_opt_landed_cost_opt_landed_cost"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="opt_landed_cost" id="opt_landed_cost.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="opt_landed_cost.menu_1" parent="opt_landed_cost.menu_root"/>
    <menuitem name="Menu 2" id="opt_landed_cost.menu_2" parent="opt_landed_cost.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="opt_landed_cost.menu_1_list" parent="opt_landed_cost.menu_1"
              action="opt_landed_cost.action_window"/>
    <menuitem name="Server to list" id="opt_landed_cost" parent="opt_landed_cost.menu_2"
              action="opt_landed_cost.action_server"/>
    -->
  </data>
</odoo>